<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>JavaScript Variables Lesson</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <h1>📘 JavaScript Variables: Learn by Example</h1>

  <p>In JavaScript, variables are used to store data values. You can declare them using <code>let</code>, <code>const</code>, or <code>var</code>.</p>

  <h2>1. Declaring a Variable with <code>let</code></h2>
  <pre>
let name = "Tumiso";
let age = 25;
  </pre>
  <button onclick="exampleLet()">Try it</button>
  <div id="outputLet" class="output"></div>

  <h2>2. Using <code>const</code> for Constants</h2>
  <pre>
const PI = 3.14159;
  </pre>
  <button onclick="exampleConst()">Try it</button>
  <div id="outputConst" class="output"></div>

  <h2>3. Reassigning a Variable</h2>
  <pre>
let counter = 0;
counter = counter + 1;
  </pre>
  <button onclick="exampleReassign()">Try it</button>
  <div id="outputReassign" class="output"></div>

  <h2>4. Mixing Strings and Numbers</h2>
  <pre>
let firstName = "Tumiso";
let score = 95;
  </pre>
  <button onclick="exampleMix()">Try it</button>
  <div id="outputMix" class="output"></div>

  <h2>5. Summary</h2>
  <ul>
    <li><code>let</code> is used for variables that can change.</li>
    <li><code>const</code> is used for variables that should not change.</li>
    <li><code>var</code> is old and less recommended (used in older JavaScript).</li>
  </ul>

  <p>Practice writing your own below </p>
  <textarea id="codeEditor" rows="5" cols="60">// Write your code here</textarea><br>
  <button onclick="runUserCode()">Run My Code</button>
  <div id="outputUserCode"></div>

  <script src="script.js"></script>
</body>
</html>



#######

body {
  font-family: Arial, sans-serif;
  margin: 40px;
  background: #f9f9f9;
}

h1, h2 {
  color: #333;
}

pre {
  background: #eee;
  padding: 10px;
  border-radius: 5px;
}

.output {
  background-color: #ddd;
  padding: 15px;
  margin-top: 10px;
  border-radius: 5px;
}

button {
  padding: 10px 20px;
  font-size: 16px;
  margin-top: 10px;
}


####

function exampleLet() {
  let name = "Tumiso";
  let age = 25;
  document.getElementById("outputLet").innerText = `Name: ${name}, Age: ${age}`;
}

function exampleConst() {
  const PI = 3.14159;
  document.getElementById("outputConst").innerText = `Value of PI: ${PI}`;
}

function exampleReassign() {
  let counter = 0;
  counter = counter + 1;
  document.getElementById("outputReassign").innerText = `Counter after update: ${counter}`;
}

function exampleMix() {
  let firstName = "Tumiso";
  let score = 95;
  let message = firstName + " scored " + score + "% in JavaScript!";
  document.getElementById("outputMix").innerText = message;
}

function runUserCode() {
  const code = document.getElementById("codeEditor").value;
  try {
    const result = eval(code);
    document.getElementById("outputUserCode").innerText = result !== undefined ? result : "Code ran successfully!";
  } catch (err) {
    document.getElementById("outputUserCode").innerText = "Error: " + err.message;
  }
}
